---
import Layout from '@layouts/Layout.astro';
import Dashboard from '@templates/Dashboard.astro';
import type { Meta } from '@scripts/type/meta';

const { default: pagesData } = await import('../assets/data/pages.json');
const pages = pagesData.pages as Meta[];
---

<Layout>
  <Dashboard pages={pages} />
</Layout>
