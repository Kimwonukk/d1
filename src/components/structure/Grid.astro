---
interface Props {
  cols?: 1 | 2 | 3 | 4;
  gap?: 'sm' | 'md' | 'lg';
}

const { cols = 3, gap = 'md' } = Astro.props;
---

<div class:list={['grid', `cols-${cols}`, `gap-${gap}`]}>
  <slot />
</div>

<style is:global lang="scss"></style>
